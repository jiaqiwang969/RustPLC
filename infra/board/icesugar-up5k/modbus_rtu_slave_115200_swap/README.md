# iCESugar UP5K Modbus RTU Slave (TX/RX swapped mapping)

This is the same logic as `modbus_rtu_slave_115200`, but with serial pins swapped:

- `RX` -> FPGA pin `6`
- `TX` -> FPGA pin `4`

Use this image only for board-level serial routing diagnosis.
